const DEPS = [
    "ts-pattern",
] as const;

const DEV_DEPS = [
    "@antfu/eslint-config",
    "eslint",
    "rolldown",
    "tsx",
    "typescript",
] as const;

export {
    DEPS,
    DEV_DEPS,
};
