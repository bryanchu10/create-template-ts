const DEPS = [
    "ts-pattern",
    "neverthrow",
] as const;

const DEV_DEPS = [
    "@antfu/eslint-config",
    "eslint",
    "rolldown",
    "tsx",
    "typescript",
] as const;

export {
    DEPS,
    DEV_DEPS,
};
